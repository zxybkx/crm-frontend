import React, {PureComponent} from 'react';
import {Row, Col, Table, Button, Input, Collapse} from 'antd';

export default class RoleManage extends PureComponent {
   render () {
     return (
       <div>未开发</div>
     )
   }
}
